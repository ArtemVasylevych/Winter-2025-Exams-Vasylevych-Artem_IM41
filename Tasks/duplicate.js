'use strict';

duplicate = (value, number) => {
  if (number <= 0) return [];
    res = [];
    for (let i = 0; i < N; i++) {
      res[i] = value;
    }
    return res;
};

module.exports = duplicate;
