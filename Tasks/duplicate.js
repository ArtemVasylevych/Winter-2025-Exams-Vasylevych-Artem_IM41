'use strict';

const duplicate = (value, number) => {
  if (number <= 0) return [];
    let res = [];
    for (let i = 0; i < N; i++) {
      res[i] = value;
    }
    return res;
};

module.exports = duplicate;
